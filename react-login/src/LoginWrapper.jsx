// FILE PURPOSE: The entry file exposed via Module Federation so Vue can import the Login component.
// Wrapper component that exports the main feature for Module Federation
import LoginForm from './components/loginForm'

export default LoginForm
